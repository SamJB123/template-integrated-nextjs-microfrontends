/**
 * Optional config for generate-route-stubs.ts
 *
 * hostApp: workspace-relative path to the Next.js application that will act as the single build target.
 */
export default {
  hostApp: 'apps/web',
} as const;
