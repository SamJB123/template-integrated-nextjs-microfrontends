# ---------------------------
# Environment Variables File
# ---------------------------
# ROUTE_REGISTRY_SNAPSHOT
# When set to "true" the route-stub generator will also write a JSON snapshot
# of the in-memory route registry to apps/web/.generated/route-registry.json.
# Leave unset or set to "false" to skip writing the snapshot.

ROUTE_REGISTRY_SNAPSHOT=false
